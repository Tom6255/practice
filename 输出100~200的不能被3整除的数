//输出100~200的不能被3整除的数
#include <stdio.h>
int main(void)
{
	int num;
	for (num = 100; num <= 200; num++)
	{
		if (num % 3 == 0)
		{
			continue;
		}
		printf("%d ", num);
	}
	printf("\n");
	return 0;
}
